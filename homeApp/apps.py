from django.apps import AppConfig

# This is the configuration file for the app. It is used to define the app name and other configurations.
class HomeappConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'homeApp'
